{
  "name": "eduaid-bot-deployment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd eduaid-bot && npm ci && npm run build",
    "start": "cd eduaid-bot && npm start",
    "vercel-build": "cd eduaid-bot && npm ci && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
